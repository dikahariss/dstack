---
name: calib-default
description: Fixture omitting calibration so the parser default of workflow is exercised end to end through to the rendered frontmatter.
allowed-tools: Read
metadata:
  dstack:
    version: 0.1.0
    type: semantic
    context_budget_tokens: 1000
---
# /calib-default

1. A spine step so the body is not flagged for the missing-spine warning.
