{"prompt": "Review this diff:\n```diff\n- function getUser(id) {\n-   return db.query(`SELECT * FROM users WHERE id = ${id}`);\n- }\n+ async function getUser(id) {\n+   if (!id) return null;\n+   return await db.query('SELECT * FROM users WHERE id = ?', [id]);\n+ }\n```\nWhat are the issues and improvements? Be concrete about what to call out and what to praise.", "anti_pattern": "Listing every change as equally important without prioritising security (SQL injection fix) above stylistic ones."}
{"prompt": "I'm reviewing a 600-line PR that adds a new auth flow plus refactors the user-profile component plus updates 3 dependency versions. The author asks for review approval. What's my response?", "anti_pattern": "Approving despite the scope problem, or rejecting without offering a path forward."}
